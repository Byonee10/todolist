const item = (id,title,description,dueDate,priority,completed) =>{
    return {
        id,
        title,
        description,
        dueDate,
        priority,
        completed
    }
} 

export default item;